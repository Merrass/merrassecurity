// JavaScript code for parallax effect (assuming library is named "Parallax")

window.onload = function() {
    // Initialize parallax effect on the element with ID "scene"
    new Parallax(document.getElementById('scene'));
};